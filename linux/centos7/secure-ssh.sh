#Secure-ssh.sh
#author Blaise
#creates a new ssh user using $1 parameter
#adds a public key from local repo or curled from the remote repo
#removes roots ability to ssh in
echo "this is where the code would go if I knew what this was meant to do"
